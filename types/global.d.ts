// Ambient module declarations for packages without bundled types (safety net)
// Remove entries if @types/* packages are installed later.

declare module "swiper/react";
declare module "swiper/modules";
